/* const message1 = 'Hello, How are you?';
function reverseString(text) {
    var store = '';
    for (var element of text) {
        store = element + store;
    }
    return store;
}
var result = reverseString(message1);
console.log(result); */


// practice reverse string
/* function reverseString(text) {
    var store = '';
    for (var element of text) {
        store = element + store;
    }
    return store;
}
var message2 = 'I love you!';
var result = reverseString(message2);
console.log(result); */


// practice reverse string one more
function reverseString(text) {
    var store = '';
    for (var element of text) {
        store = element + store;
    }
    return store;
}
var message3 = 'I miss you!'
var result = reverseString(message3);
console.log(result);